import React from 'react';

const NextPage: React.FC = () => {
    return (
        <div>
            <h1>다음 화면입니다!</h1>
            <p>여기에서 추가 내용을 넣을 수 있습니다.</p>
        </div>
    );
};

export default NextPage;
